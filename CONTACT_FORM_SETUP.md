# Контактная форма - Настройка

## ✅ Реализовано

Контактная форма на странице `/contact` полностью настроена и готова к работе. Все письма будут отправляться на `info@oson-ish.uz`.

### Что работает:

1. ✅ Форма контактов с полями:
   - Имя
   - Телефон
   - Тема обращения
   - Сообщение

2. ✅ API endpoint `/api/contact` для обработки формы
3. ✅ Email сервис с поддержкой nodemailer
4. ✅ Валидация данных на клиенте и сервере
5. ✅ Отображение сообщений об успехе/ошибке
6. ✅ Кнопка "Помощь" в хедере ведет на `/contact`

## 📧 Настройка SMTP

Чтобы письма реально отправлялись, нужно настроить SMTP параметры в файле `.env`:

```env
# SMTP Configuration
SMTP_HOST=ваш_smtp_сервер.com          # Адрес SMTP сервера
SMTP_PORT=587                           # Порт (587 для TLS, 465 для SSL)
SMTP_USER=info@oson-ish.uz             # Email для авторизации
SMTP_PASSWORD=ваш_smtp_пароль           # Пароль SMTP
SMTP_FROM=info@oson-ish.uz             # Email отправителя
```

### Пример настройки для популярных провайдеров:

#### Gmail
```env
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=info@oson-ish.uz
SMTP_PASSWORD=ваш_пароль_приложения
SMTP_FROM=info@oson-ish.uz
```

#### Яндекс
```env
SMTP_HOST=smtp.yandex.ru
SMTP_PORT=465
SMTP_USER=info@oson-ish.uz
SMTP_PASSWORD=ваш_пароль
SMTP_FROM=info@oson-ish.uz
```

#### Mail.ru
```env
SMTP_HOST=smtp.mail.ru
SMTP_PORT=465
SMTP_USER=info@oson-ish.uz
SMTP_PASSWORD=ваш_пароль
SMTP_FROM=info@oson-ish.uz
```

#### Собственный SMTP сервер
Используйте параметры, предоставленные вашим хостинг-провайдером.

## 🔧 Тестирование

### Без SMTP (режим разработки)

Если SMTP не настроен, письма будут логироваться в консоль сервера. Это удобно для разработки.

Запустите сервер и откройте логи:
```bash
npm run dev
```

В консоли вы увидите:
```
⚠️  SMTP не настроен. Email будет залогирован в консоль.
📧 EMAIL TO: info@oson-ish.uz
📧 SUBJECT: Новое сообщение: Техническая поддержка
📧 BODY: 
Новое сообщение с формы обратной связи

Имя: Тестовый пользователь
Телефон: +998 90 123 45 67
Тема: Техническая поддержка
Сообщение:
Это тестовое сообщение...
```

### С настроенным SMTP

После настройки SMTP письма будут реально отправляться на `info@oson-ish.uz`.

Проверьте:
1. Откройте http://localhost:3000/contact
2. Заполните форму
3. Нажмите "Отправить сообщение"
4. Проверьте почту `info@oson-ish.uz`

## 📝 Структура файлов

```
osonish-website/
├── app/
│   ├── api/
│   │   └── contact/
│   │       └── route.ts              # API endpoint для формы
│   └── contact/
│       └── page.tsx                  # Страница контактов
├── src/
│   ├── components/
│   │   └── contact/
│   │       └── contact-form.tsx      # Компонент формы (client)
│   └── lib/
│       └── services/
│           └── emailService.ts       # Сервис отправки email
└── .env                              # Конфигурация SMTP
```

## 🚀 После настройки SMTP

После того как вы укажете правильные SMTP параметры:

1. Перезапустите сервер разработки:
   ```bash
   npm run dev
   ```

2. Проверьте отправку тестового письма через форму

3. Если письмо не приходит:
   - Проверьте логи сервера на наличие ошибок
   - Убедитесь, что SMTP параметры верны
   - Проверьте, что почтовый сервер разрешает отправку с вашего IP
   - Проверьте папку "Спам" в почте

## 🔒 Безопасность

⚠️ **ВАЖНО**: Не коммитьте файл `.env` в Git! Он уже добавлен в `.gitignore`.

Для продакшена используйте переменные окружения через панель хостинга (Vercel, Railway и т.д.).

## 📞 Куда приходят письма

Все письма с контактной формы отправляются на: **info@oson-ish.uz**

Формат письма:
```
Тема: Новое сообщение: [Выбранная тема]

Новое сообщение с формы обратной связи

Имя: [Имя пользователя]
Телефон: [Телефон пользователя]
Тема: [Тема обращения]
Сообщение:
[Текст сообщения]

---
Отправлено с формы контактов Oson Ish
```

## ✅ Готово к использованию

После настройки SMTP система полностью готова к работе и будет автоматически отправлять все сообщения с контактной формы на указанный email адрес.

