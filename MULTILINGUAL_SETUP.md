# Настройка мультиязычности на сайте Osonish

## 📋 Обзор

Сайт Osonish теперь полностью поддерживает два языка:
- 🇷🇺 **Русский** (по умолчанию) - код: `ru`
- 🇺🇿 **Узбекский** - код: `uz`

## 🔧 Технологии

Используется библиотека **next-intl** для интернационализации Next.js приложения.

## 📁 Структура файлов

### 1. Файлы переводов
```
/messages/
├── ru.json  # Русские переводы
└── uz.json  # Узбекские переводы
```

### 2. Конфигурация i18n
```
/src/i18n/
├── routing.ts   # Настройка маршрутизации
└── request.ts   # Загрузка переводов
```

### 3. Компоненты
```
/src/components/shared/
└── language-switcher.tsx  # Компонент переключения языка
```

## 🌐 URL структура

Сайт использует локаль в URL:
- Русский (по умолчанию): `https://osonish.uz/` или `https://osonish.uz/ru/`
- Узбекский: `https://osonish.uz/uz/`

Примеры:
- `/orders` → `/uz/orders`
- `/vacancies` → `/uz/vacancies`
- `/about` → `/uz/about`

## 📝 Структура переводов

В файлах `ru.json` и `uz.json` переводы организованы по разделам:

```json
{
  "common": {
    "login": "Войти",
    "help": "Помощь",
    ...
  },
  "navigation": {
    "orders": "Заказы",
    "vacancies": "Вакансии",
    ...
  },
  "auth": { ... },
  "dashboard": { ... },
  ...
}
```

## 💻 Использование в компонентах

### Client Components

```tsx
'use client';
import { useTranslations } from 'next-intl';

export function MyComponent() {
  const t = useTranslations('navigation');
  
  return <div>{t('orders')}</div>;
}
```

### Server Components

```tsx
import { useTranslations } from 'next-intl';

export default function MyPage() {
  const t = useTranslations('common');
  
  return <h1>{t('login')}</h1>;
}
```

## 🔄 Переключатель языка

Компонент `LanguageSwitcher` находится в header'е сайта и позволяет:
- Показывает текущий язык с флагом
- Открывает выпадающее меню со списком языков
- Переключает язык с сохранением текущей страницы
- Показывает индикатор загрузки при переключении

## ⚙️ Middleware

Middleware (`middleware.ts`) обрабатывает:
1. **Интернационализацию** - определяет язык из URL
2. **Сессии** - проверяет аутентификацию пользователя
3. **Защищенные роуты** - редиректит на login с учетом локали

## 🆕 Добавление новых переводов

1. Откройте оба файла: `messages/ru.json` и `messages/uz.json`
2. Добавьте новый ключ в нужную секцию
3. Используйте в компоненте через `useTranslations()`

Пример:
```json
// ru.json
{
  "common": {
    "newKey": "Новый текст"
  }
}

// uz.json
{
  "common": {
    "newKey": "Yangi matn"
  }
}
```

```tsx
// В компоненте
const t = useTranslations('common');
<button>{t('newKey')}</button>
```

## 🎯 Что переведено

✅ Навигация (Заказы, Вакансии, Исполнители, О компании)
✅ Кнопки действий (Войти, Помощь, Создать заказ)
✅ Формы авторизации
✅ Личный кабинет
✅ Создание заказа
✅ Справочники (типы занятости, форматы работы, уровни опыта, города)

## 🚀 Следующие шаги

Для полной локализации сайта необходимо:
1. Перевести все страницы контента
2. Перевести мета-данные (title, description)
3. Перевести сообщения об ошибках
4. Перевести email-шаблоны
5. Перевести уведомления

## 🐛 Отладка

Если переводы не работают:
1. Проверьте, что компонент обернут в `NextIntlClientProvider`
2. Проверьте наличие ключа в обоих файлах переводов
3. Проверьте правильность пути к переводу (секция.ключ)
4. Проверьте консоль браузера на наличие ошибок

## 📚 Документация

- [next-intl](https://next-intl-docs.vercel.app/)
- [Next.js Internationalization](https://nextjs.org/docs/app/building-your-application/routing/internationalization)

