# 🚀 Быстрый старт - Мультиязычность

## Что было сделано

✅ Создан компонент переключателя языка `LanguageSwitcher`
✅ Добавлен переключатель в header сайта
✅ Обновлены файлы переводов (`ru.json`, `uz.json`)
✅ Интегрирована библиотека `next-intl`
✅ Настроен middleware для поддержки локализации
✅ Переведены основные элементы интерфейса

## 🔍 Как проверить

### 1. Запустите сервер разработки

```bash
cd osonish-website
npm run dev
```

### 2. Откройте сайт в браузере

```
http://localhost:3000
```

### 3. Найдите переключатель языка

В правом верхнем углу header'а вы увидите кнопку с иконкой глобуса 🌐 и флагом текущего языка.

### 4. Переключите язык

- Нажмите на переключатель
- Выберите нужный язык (Русский 🇷🇺 или O'zbekcha 🇺🇿)
- Страница автоматически перезагрузится с новым языком

### 5. Проверьте URL

После переключения URL должен измениться:
- Русский: `http://localhost:3000/` или `http://localhost:3000/ru/`
- Узбекский: `http://localhost:3000/uz/`

### 6. Проверьте переведенные элементы

Проверьте, что переведены:
- ✅ Пункты меню навигации (Заказы, Вакансии, Исполнители, О компании)
- ✅ Кнопка "Помощь"
- ✅ Кнопка "Войти"
- ✅ Кнопка "Создать заказ" (для заказчиков)

## 🎯 Что дальше?

### Следующие шаги по локализации:

1. **Перевести страницы контента**
   - Главная страница
   - Страницы заказов и вакансий
   - Профили исполнителей
   - Страница "О компании"

2. **Перевести формы**
   - Форма регистрации
   - Форма создания заказа
   - Форма редактирования профиля

3. **Перевести сообщения**
   - Сообщения об ошибках
   - Уведомления
   - Подсказки

4. **SEO оптимизация**
   - Мета-теги для каждого языка
   - Альтернативные URL (hreflang)
   - Локализованные sitemap

## 📝 Пример добавления нового перевода

### 1. Откройте файлы переводов

```bash
# Русский
osonish-website/messages/ru.json

# Узбекский
osonish-website/messages/uz.json
```

### 2. Добавьте новый ключ

```json
// ru.json
{
  "common": {
    "myNewButton": "Моя новая кнопка"
  }
}

// uz.json
{
  "common": {
    "myNewButton": "Mening yangi tugmam"
  }
}
```

### 3. Используйте в компоненте

```tsx
'use client';
import { useTranslations } from 'next-intl';

export function MyComponent() {
  const t = useTranslations('common');
  
  return <button>{t('myNewButton')}</button>;
}
```

## 🐛 Возможные проблемы

### Переводы не отображаются

**Решение:**
1. Проверьте, что ключ существует в обоих файлах (`ru.json` и `uz.json`)
2. Убедитесь, что компонент обернут в `NextIntlClientProvider`
3. Проверьте консоль браузера на наличие ошибок

### Переключатель языка не работает

**Решение:**
1. Убедитесь, что middleware настроен правильно
2. Проверьте, что next-intl установлен: `npm list next-intl`
3. Очистите кэш Next.js: `rm -rf .next` и перезапустите сервер

### URL не меняется при переключении

**Решение:**
1. Проверьте middleware.ts
2. Убедитесь, что роутинг настроен в `src/i18n/routing.ts`

## 📚 Полезные ссылки

- [Документация next-intl](https://next-intl-docs.vercel.app/)
- [MULTILINGUAL_SETUP.md](./MULTILINGUAL_SETUP.md) - подробная документация

## ✅ Чеклист готовности

- [x] Компонент переключателя создан
- [x] Переключатель добавлен в header
- [x] Переводы для навигации добавлены
- [x] Middleware настроен
- [x] Базовые элементы переведены
- [ ] Все страницы переведены
- [ ] Формы переведены
- [ ] Сообщения переведены
- [ ] SEO оптимизировано

