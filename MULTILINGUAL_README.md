# 🌐 Multilingual Website - Osonish

## Поддерживаемые языки

- 🇷🇺 **Русский** - Язык по умолчанию
- 🇺🇿 **O'zbekcha** - Узбекский язык

---

## Как использовать

### Переключение языка

1. Найдите иконку глобуса 🌐 в правом верхнем углу сайта
2. Нажмите на неё
3. Выберите нужный язык из выпадающего меню
4. Страница автоматически перезагрузится с выбранным языком

### URL-адреса

```bash
# Русский язык
https://osonish.uz/
https://osonish.uz/orders

# Узбекский язык  
https://osonish.uz/uz/
https://osonish.uz/uz/orders
```

---

## Для разработчиков

### Структура переводов

```
messages/
├── ru.json    # Русские переводы
└── uz.json    # Узбекские переводы
```

### Использование в компонентах

**Client Component:**
```tsx
'use client';
import { useTranslations } from 'next-intl';

export function MyComponent() {
  const t = useTranslations('navigation');
  return <div>{t('orders')}</div>;
}
```

**Server Component:**
```tsx
import { useTranslations } from 'next-intl';

export default function Page() {
  const t = useTranslations('common');
  return <h1>{t('login')}</h1>;
}
```

### Добавление новых переводов

1. Откройте `messages/ru.json` и `messages/uz.json`
2. Добавьте новый ключ в нужную секцию:

```json
// messages/ru.json
{
  "common": {
    "newButton": "Новая кнопка"
  }
}

// messages/uz.json
{
  "common": {
    "newButton": "Yangi tugma"
  }
}
```

3. Используйте в коде:
```tsx
const t = useTranslations('common');
<button>{t('newButton')}</button>
```

---

## Разделы переводов

### `common`
Общие элементы интерфейса:
- Кнопки (Войти, Сохранить, Отменить)
- Действия (Помощь, Создать заказ)
- Базовые элементы

### `navigation`
Навигационное меню:
- Заказы
- Вакансии
- Исполнители
- О компании

### `auth`
Страницы авторизации:
- Вход
- Регистрация
- Коды подтверждения

### `dashboard`
Личный кабинет:
- Кабинет заказчика
- Кабинет исполнителя
- Профиль

### Справочники
- `employmentTypes` - Типы занятости
- `workFormats` - Форматы работы
- `experienceLevels` - Уровни опыта
- `workSchedules` - Графики работы
- `salaryPeriods` - Периоды зарплаты
- `languages` - Языки
- `cities` - Города

---

## Документация

📚 Подробная документация:
- [MULTILINGUAL_SETUP.md](./MULTILINGUAL_SETUP.md) - Полное руководство
- [MULTILINGUAL_QUICKSTART.md](./MULTILINGUAL_QUICKSTART.md) - Быстрый старт
- [MULTILINGUAL_CHANGELOG.md](./MULTILINGUAL_CHANGELOG.md) - История изменений

---

## Технологии

- **next-intl** - Библиотека интернационализации для Next.js
- **Next.js 15** - React фреймворк с App Router
- **TypeScript** - Типизация

---

## Поддержка

Если у вас возникли вопросы или проблемы:
1. Проверьте документацию выше
2. Посмотрите [примеры next-intl](https://next-intl-docs.vercel.app/)
3. Проверьте консоль браузера на наличие ошибок

---

Made with ❤️ for Osonish

