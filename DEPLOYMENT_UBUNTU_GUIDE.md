# 🚀 Инструкция по деплою osonish-website на Ubuntu

Полное пошаговое руководство по развертыванию и обновлению веб-сайта Osonish на облачном сервере Ubuntu.

---

## 📋 Содержание

1. [Требования к серверу](#требования-к-серверу)
2. [Первоначальная настройка сервера](#первоначальная-настройка-сервера)
3. [Установка необходимого ПО](#установка-необходимого-по)
4. [Первый деплой приложения](#первый-деплой-приложения)
5. [Настройка Nginx](#настройка-nginx)
6. [Настройка SSL сертификата](#настройка-ssl-сертификата)
7. [Настройка PM2 для автозапуска](#настройка-pm2-для-автозапуска)
8. [Обновление приложения](#обновление-приложения)
9. [Мониторинг и логи](#мониторинг-и-логи)
10. [Решение проблем](#решение-проблем)

---

## 🖥️ Требования к серверу

### Минимальные требования:

- **OS**: Ubuntu 20.04 LTS или новее
- **RAM**: 2 GB (рекомендуется 4 GB)
- **CPU**: 2 ядра (рекомендуется 4)
- **Диск**: 20 GB свободного места
- **Порты**: 80 (HTTP), 443 (HTTPS), 22 (SSH)

### Рекомендуемые провайдеры:

- DigitalOcean (Droplet)
- AWS EC2
- Google Cloud Compute Engine
- Hetzner Cloud
- Timeweb Cloud

---

## 🔧 Первоначальная настройка сервера

### 1. Подключение к серверу

```bash
ssh root@your-server-ip
```

### 2. Обновление системы

```bash
# Обновление списка пакетов
sudo apt update

# Обновление установленных пакетов
sudo apt upgrade -y

# Установка базовых утилит
sudo apt install -y curl wget git build-essential
```

### 3. Создание пользователя для деплоя

```bash
# Создание пользователя
sudo adduser deploy

# Добавление sudo прав
sudo usermod -aG sudo deploy

# Переключение на пользователя deploy
su - deploy
```

### 4. Настройка SSH ключа (опционально, но рекомендуется)

На вашем локальном компьютере:

```bash
# Генерация SSH ключа (если нет)
ssh-keygen -t rsa -b 4096

# Копирование ключа на сервер
ssh-copy-id deploy@your-server-ip
```

---

## 📦 Установка необходимого ПО

### 1. Установка Node.js (v20 LTS)

```bash
# Установка Node.js через NodeSource
curl -fsSL https://deb.nodesource.com/setup_20.x | sudo -E bash -
sudo apt install -y nodejs

# Проверка установки
node --version  # должно быть v20.x.x
npm --version   # должно быть 10.x.x
```

### 2. Установка PM2 (Process Manager)

```bash
# Установка PM2 глобально
sudo npm install -g pm2

# Проверка установки
pm2 --version
```

### 3. Установка Nginx

```bash
# Установка Nginx
sudo apt install -y nginx

# Запуск и включение автозапуска
sudo systemctl start nginx
sudo systemctl enable nginx

# Проверка статуса
sudo systemctl status nginx
```

### 4. Установка Certbot для SSL (Let's Encrypt)

```bash
# Установка Certbot
sudo apt install -y certbot python3-certbot-nginx

# Проверка установки
certbot --version
```

---

## 🚀 Первый деплой приложения

### 1. Клонирование репозитория

```bash
# Переход в домашнюю директорию
cd ~

# Создание директории для проектов
mkdir -p ~/projects
cd ~/projects

# Клонирование репозитория
git clone https://github.com/farakor/osonish-website.git
cd osonish-2/osonish-website

# Или если используете приватный репозиторий с SSH:
# git clone git@github.com:your-username/osonish-2.git
```

### 2. Установка зависимостей

```bash
# Установка npm пакетов
npm install

# Это может занять 2-5 минут
```

### 3. Настройка переменных окружения

```bash
# Создание .env.local файла (Next.js использует этот файл для production на сервере)
nano .env.local

# Добавьте следующие переменные:
```

Содержимое `.env.local`:

```env
# Supabase Configuration
NEXT_PUBLIC_SUPABASE_URL=https://ruuseponrzonvyyqpdsc.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_anon_key_here
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key_here

# Eskiz SMS Configuration
ESKIZ_SMS_EMAIL=info@oson-ish.uz
ESKIZ_SMS_PASSWORD=your_password_here
NEXT_PUBLIC_ESKIZ_SMS_URL=https://notify.eskiz.uz/api
SMS_SENDER_NAME=OsonIsh

# App Configuration
NEXT_PUBLIC_APP_URL=https://oson-ish.uz
NODE_ENV=production

# Email Configuration (если используется)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your_email@gmail.com
SMTP_PASSWORD=your_app_password
```

**Важно:** Next.js использует `.env.local` для локальных переменных окружения. Этот файл не коммитится в Git (должен быть в .gitignore) и используется для всех окружений на данной машине.

Сохраните файл: `Ctrl + X`, затем `Y`, затем `Enter`

### 4. Сборка приложения

```bash
# Сборка Next.js приложения
npm run build

# Это может занять 3-10 минут в зависимости от размера проекта
```

### 5. Запуск приложения через PM2

```bash
# Создание ecosystem файла для PM2
nano ecosystem.config.js
```

Содержимое `ecosystem.config.js`:

```javascript
module.exports = {
  apps: [
    {
      name: "osonish-website",
      script: "npm",
      args: "start",
      cwd: "/home/deploy/projects/osonish-website",
      instances: "max",
      exec_mode: "cluster",
      env: {
        NODE_ENV: "production",
        PORT: 3000,
      },
      error_file: "./logs/err.log",
      out_file: "./logs/out.log",
      log_file: "./logs/combined.log",
      time: true,
      max_memory_restart: "1G",
      autorestart: true,
      watch: false,
    },
  ],
};
```

```bash
# Создание директории для логов
mkdir -p logs

# Запуск приложения через PM2
pm2 start ecosystem.config.js

# Сохранение конфигурации PM2
pm2 save

# Настройка автозапуска PM2 при загрузке системы
pm2 startup
# Выполните команду, которую выведет pm2 startup (обычно начинается с sudo)

# Проверка статуса
pm2 status
pm2 logs osonish-website
```

---

## 🌐 Настройка Nginx

### 1. Создание конфигурации Nginx

```bash
# Создание файла конфигурации
sudo nano /etc/nginx/sites-available/osonish-website
```

Содержимое конфигурации:

```nginx
# HTTP конфигурация (будет перенаправлять на HTTPS после установки SSL)
server {
    listen 80;
    listen [::]:80;
    server_name oson-ish.uz www.oson-ish.uz;

    # Размер загружаемых файлов
    client_max_body_size 10M;

    # Логи
    access_log /var/log/nginx/osonish-website-access.log;
    error_log /var/log/nginx/osonish-website-error.log;

    # Прокси к Next.js приложению
    location / {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
        proxy_read_timeout 300;
        proxy_connect_timeout 300;
    }

    # Кеширование статических файлов
    location /_next/static {
        proxy_pass http://localhost:3000;
        proxy_cache_valid 60m;
        add_header Cache-Control "public, max-age=3600, immutable";
    }

    # Кеширование изображений Next.js
    location /_next/image {
        proxy_pass http://localhost:3000;
        proxy_cache_valid 60m;
    }
}
```

### 2. Активация конфигурации

```bash
# Создание символической ссылки
sudo ln -s /etc/nginx/sites-available/osonish-website /etc/nginx/sites-enabled/

# Удаление дефолтной конфигурации
sudo rm /etc/nginx/sites-enabled/default

# Проверка конфигурации Nginx
sudo nginx -t

# Перезагрузка Nginx
sudo systemctl reload nginx
```

### 3. Настройка firewall (если используется)

```bash
# Разрешение HTTP и HTTPS трафика
sudo ufw allow 'Nginx Full'
sudo ufw allow OpenSSH
sudo ufw enable
sudo ufw status
```

Теперь сайт должен быть доступен по IP адресу вашего сервера на порту 80.

---

## 🔒 Настройка SSL сертификата

### 1. Настройка DNS

Перед установкой SSL убедитесь, что ваш домен указывает на IP сервера:

**A записи:**

- `@` → `your-server-ip` (для oson-ish.uz)
- `www` → `your-server-ip` (для www.oson-ish.uz)

Проверка DNS:

```bash
nslookup oson-ish.uz
nslookup www.oson-ish.uz
```

### 2. Установка SSL сертификата

```bash
# Получение сертификата от Let's Encrypt
sudo certbot --nginx -d oson-ish.uz -d www.oson-ish.uz

# Следуйте инструкциям:
# 1. Введите email для уведомлений
# 2. Согласитесь с Terms of Service (Y)
# 3. Выберите опцию 2 (перенаправление HTTP на HTTPS)
```

Certbot автоматически:

- Получит SSL сертификат
- Настроит Nginx для HTTPS
- Настроит автоматическое перенаправление с HTTP на HTTPS

### 3. Проверка автообновления сертификата

```bash
# Тест автообновления
sudo certbot renew --dry-run

# Если всё работает, сертификат будет автоматически обновляться
```

### 4. Проверка HTTPS

Откройте в браузере:

- https://oson-ish.uz
- https://www.oson-ish.uz

Должен появиться замочек безопасности.

---

## ⚙️ Настройка PM2 для автозапуска

PM2 позволяет автоматически запускать приложение при перезагрузке сервера.

```bash
# Сохранение текущих процессов PM2
pm2 save

# Генерация скрипта автозапуска
pm2 startup

# Выполните команду, которую выведет pm2 startup
# Пример: sudo env PATH=$PATH:/usr/bin /usr/lib/node_modules/pm2/bin/pm2 startup systemd -u deploy --hp /home/deploy

# Проверка автозапуска (перезагрузка сервера)
sudo reboot

# После перезагрузки подключитесь и проверьте
ssh deploy@your-server-ip
pm2 status
# Приложение должно быть запущено автоматически
```

---

## 🔄 Обновление приложения

Когда вы внесли изменения в код и хотите обновить приложение на сервере:

### Метод 1: Простое обновление через Git (рекомендуется)

```bash
# 1. Подключитесь к серверу
ssh deploy@your-server-ip

# 2. Перейдите в директорию проекта
cd ~/projects/osonish-website

# 3. Создайте резервную копию .env файла
cp .env .env.backup

# 4. Получите последние изменения из репозитория
git pull origin main  # или название вашей ветки

# 5. Установите обновленные зависимости (если были изменения в package.json)
npm install

# 6. Пересоберите приложение
npm run build

# 7. Перезапустите приложение через PM2
pm2 restart osonish-website

# 8. Проверьте логи на наличие ошибок
pm2 logs osonish-website --lines 50

# 9. Проверьте статус
pm2 status
```

### Метод 2: Автоматизация с помощью скрипта

Создайте скрипт для автоматического обновления:

```bash
# Создание скрипта обновления
nano ~/update-website.sh
```

Содержимое `update-website.sh`:

```bash
#!/bin/bash

# Цвета для вывода
GREEN='\033[0;32m'
RED='\033[0;31m'
YELLOW='\033[1;33m'
NC='\033[0m' # No Color

PROJECT_DIR="/home/deploy/projects/osonish-website"
APP_NAME="osonish-website"

echo -e "${GREEN}========================================${NC}"
echo -e "${GREEN}🚀 Обновление Osonish Website${NC}"
echo -e "${GREEN}========================================${NC}"

# Переход в директорию проекта
cd $PROJECT_DIR || exit 1

# Резервная копия .env
echo -e "${YELLOW}📦 Создание резервной копии конфигурации...${NC}"
cp .env.production .env.production.backup.$(date +%Y%m%d_%H%M%S)

# Получение изменений из Git
echo -e "${YELLOW}📥 Получение последних изменений...${NC}"
git fetch origin
git pull origin main

# Проверка изменений в package.json
if git diff HEAD@{1} HEAD -- package.json | grep -q .; then
    echo -e "${YELLOW}📦 Обнаружены изменения в зависимостях, установка...${NC}"
    npm install
else
    echo -e "${GREEN}✓ Изменений в зависимостях не обнаружено${NC}"
fi

# Сборка приложения
echo -e "${YELLOW}🔨 Сборка приложения...${NC}"
if npm run build; then
    echo -e "${GREEN}✓ Сборка завершена успешно${NC}"
else
    echo -e "${RED}✗ Ошибка при сборке${NC}"
    exit 1
fi

# Перезапуск PM2
echo -e "${YELLOW}🔄 Перезапуск приложения...${NC}"
pm2 restart $APP_NAME

# Ожидание запуска
sleep 3

# Проверка статуса
echo -e "${YELLOW}🔍 Проверка статуса...${NC}"
pm2 status $APP_NAME

# Показ последних логов
echo -e "${YELLOW}📋 Последние логи:${NC}"
pm2 logs $APP_NAME --lines 20 --nostream

echo -e "${GREEN}========================================${NC}"
echo -e "${GREEN}✓ Обновление завершено!${NC}"
echo -e "${GREEN}========================================${NC}"
```

Сделайте скрипт исполняемым:

```bash
chmod +x ~/update-website.sh
```

Теперь для обновления достаточно выполнить:

```bash
~/update-website.sh
```

### Метод 3: Zero-downtime обновление

Для обновления без простоя используйте reload вместо restart:

```bash
cd ~/projects/osonish-2/osonish-website
git pull origin main
npm install
npm run build
pm2 reload osonish-website  # Плавное обновление без остановки
```

---

## 📊 Мониторинг и логи

### Команды PM2

```bash
# Просмотр статуса всех приложений
pm2 status

# Просмотр логов в реальном времени
pm2 logs osonish-website

# Просмотр последних 100 строк логов
pm2 logs osonish-website --lines 100

# Просмотр только ошибок
pm2 logs osonish-website --err

# Очистка логов
pm2 flush

# Детальная информация о приложении
pm2 info osonish-website

# Мониторинг ресурсов
pm2 monit
```

### Логи Nginx

```bash
# Просмотр логов доступа
sudo tail -f /var/log/nginx/osonish-website-access.log

# Просмотр логов ошибок
sudo tail -f /var/log/nginx/osonish-website-error.log

# Поиск ошибок 500
sudo grep " 500 " /var/log/nginx/osonish-website-access.log
```

### Системные ресурсы

```bash
# Использование памяти
free -h

# Использование диска
df -h

# Загрузка процессора
top
# или
htop  # (нужно установить: sudo apt install htop)

# Сетевые подключения
sudo netstat -tulpn | grep LISTEN
```

### Настройка мониторинга через PM2 Plus (опционально)

```bash
# Регистрация на https://app.pm2.io/
# Получите ключ и выполните:
pm2 link [secret_key] [public_key]

# Теперь можете мониторить приложение через веб-интерфейс
```

---

## 🔧 Решение проблем

### Проблема 1: Ошибка "supabaseUrl is required" при сборке

**Симптомы:**

```
Error: supabaseUrl is required.
> Build error occurred
```

**Причина:**
Next.js не видит переменные окружения во время сборки.

**Решение:**

```bash
# Убедитесь что файл называется .env.local (а НЕ .env.production)
ls -la | grep .env

# Если файл называется .env.production, переименуйте его
mv .env.production .env.local

# Проверьте содержимое файла
cat .env.local

# Убедитесь что NEXT_PUBLIC_SUPABASE_URL заполнен
grep NEXT_PUBLIC_SUPABASE_URL .env.local

# Пересоберите приложение
npm run build
```

**Альтернативное решение:**
Если хотите использовать `.env.production`, создайте также `.env.local` с теми же переменными, или используйте `.env.production.local`:

```bash
# Вариант 1: Скопировать в .env.local
cp .env.production .env.local

# Вариант 2: Переименовать в .env.production.local
mv .env.production .env.production.local

# Затем пересобрать
npm run build
```

### Проблема 2: Приложение не запускается

**Симптомы:**

```bash
pm2 status
# Status: errored
```

**Решение:**

```bash
# Проверьте логи
pm2 logs osonish-website --err

# Проверьте переменные окружения
cat .env.local

# Попробуйте запустить вручную
npm start

# Проверьте порт 3000
sudo lsof -i :3000
# Если порт занят, убейте процесс:
sudo kill -9 [PID]
```

### Проблема 2: Nginx показывает 502 Bad Gateway

**Причины:**

- Next.js приложение не запущено
- Неправильный порт в конфигурации Nginx

**Решение:**

```bash
# Проверьте статус PM2
pm2 status

# Если не запущено, запустите
pm2 start ecosystem.config.js

# Проверьте, что приложение слушает на порту 3000
curl http://localhost:3000

# Проверьте конфигурацию Nginx
sudo nginx -t

# Перезагрузите Nginx
sudo systemctl reload nginx
```

### Проблема 3: Ошибки при сборке (npm run build)

**Симптомы:**

```
Error: Cannot find module ...
Type errors ...
```

**Решение:**

```bash
# Удалите node_modules и lock файлы
rm -rf node_modules package-lock.json

# Очистите npm кеш
npm cache clean --force

# Установите зависимости заново
npm install

# Попробуйте собрать снова
npm run build
```

### Проблема 4: Недостаточно памяти при сборке

**Симптомы:**

```
FATAL ERROR: Ineffective mark-compacts near heap limit Allocation failed - JavaScript heap out of memory
```

**Решение:**

```bash
# Увеличьте лимит памяти для Node.js
export NODE_OPTIONS="--max-old-space-size=4096"

# Или в ecosystem.config.js добавьте:
node_args: '--max-old-space-size=4096'

# Затем пересоберите
npm run build
```

### Проблема 5: SSL сертификат не обновляется

**Решение:**

```bash
# Проверьте статус сертификата
sudo certbot certificates

# Попробуйте обновить вручную
sudo certbot renew --force-renewal

# Проверьте cron задачу
sudo systemctl status certbot.timer

# Включите автообновление если выключено
sudo systemctl enable certbot.timer
```

### Проблема 6: Сайт не открывается после обновления

**Решение:**

```bash
# 1. Проверьте логи PM2
pm2 logs osonish-website --lines 50

# 2. Проверьте логи Nginx
sudo tail -n 50 /var/log/nginx/osonish-website-error.log

# 3. Откатитесь к предыдущей версии
cd ~/projects/osonish-2/osonish-website
git log --oneline -5  # Посмотрите последние коммиты
git reset --hard [commit-hash]  # Откат к рабочей версии
npm install
npm run build
pm2 restart osonish-website

# 4. Восстановите .env из бэкапа если нужно
cp .env.local.backup .env.local
```

### Проблема 7: Высокое использование CPU/памяти

**Решение:**

```bash
# Проверьте использование ресурсов
pm2 monit

# Перезапустите приложение
pm2 restart osonish-website

# Если проблема сохраняется, увеличьте ресурсы сервера
# или оптимизируйте код приложения
```

---

## 📝 Чеклист деплоя

### Первый деплой

- [ ] Сервер настроен и обновлен
- [ ] Установлены Node.js, PM2, Nginx
- [ ] Репозиторий склонирован
- [ ] Установлены зависимости (npm install)
- [ ] Создан и настроен .env.local с правильными значениями
- [ ] NEXT_PUBLIC_SUPABASE_URL заполнен
- [ ] NEXT_PUBLIC_SUPABASE_ANON_KEY заполнен
- [ ] SUPABASE_SERVICE_ROLE_KEY заполнен
- [ ] Приложение собрано (npm run build) без ошибок
- [ ] PM2 настроен и запущен
- [ ] Nginx настроен и перезагружен
- [ ] DNS записи настроены
- [ ] SSL сертификат установлен
- [ ] Автозапуск PM2 настроен
- [ ] Сайт открывается по HTTPS
- [ ] Все функции работают корректно

### Обновление

- [ ] Создана резервная копия .env
- [ ] Получены последние изменения (git pull)
- [ ] Обновлены зависимости (если нужно)
- [ ] Приложение пересобрано
- [ ] PM2 перезапущен
- [ ] Проверены логи на ошибки
- [ ] Сайт работает корректно
- [ ] Все функции проверены

---

## 🎯 Полезные команды

### Быстрая диагностика

```bash
# Одной командой проверить всё
pm2 status && sudo systemctl status nginx && free -h && df -h
```

### Полная перезагрузка

```bash
# Если что-то пошло не так
cd ~/projects/osonish-2/osonish-website
pm2 delete osonish-website
pm2 start ecosystem.config.js
sudo systemctl restart nginx
```

### Резервное копирование

```bash
# Создание бэкапа приложения
cd ~/projects/osonish-2
tar -czf osonish-website-backup-$(date +%Y%m%d).tar.gz osonish-website/ --exclude=node_modules --exclude=.next

# Не забудьте включить .env.local в бэкап!
# Или сделайте отдельный бэкап .env
cp osonish-website/.env.local ~/backups/.env.local.backup.$(date +%Y%m%d)

# Скачивание бэкапа на локальный компьютер
# На вашем компьютере:
scp deploy@your-server-ip:~/projects/osonish-2/osonish-website-backup-*.tar.gz .
```

---

## 🔐 Безопасность

### Рекомендации:

1. **Регулярно обновляйте систему:**

```bash
sudo apt update && sudo apt upgrade -y
```

2. **Настройте fail2ban для защиты от брутфорса:**

```bash
sudo apt install fail2ban -y
sudo systemctl enable fail2ban
```

3. **Используйте SSH ключи вместо паролей**

4. **Ограничьте SSH доступ:**

```bash
sudo nano /etc/ssh/sshd_config
# Измените:
PermitRootLogin no
PasswordAuthentication no
```

5. **Регулярно проверяйте логи безопасности:**

```bash
sudo tail -f /var/log/auth.log
```

---

## 📞 Поддержка

При возникновении проблем:

1. Проверьте этот документ в разделе [Решение проблем](#решение-проблем)
2. Изучите логи PM2 и Nginx
3. Проверьте [документацию Next.js](https://nextjs.org/docs)
4. Свяжитесь с командой разработки

---

## 📚 Дополнительные ресурсы

- [Next.js Deployment](https://nextjs.org/docs/deployment)
- [PM2 Documentation](https://pm2.keymetrics.io/docs/usage/quick-start/)
- [Nginx Documentation](https://nginx.org/en/docs/)
- [Let's Encrypt Documentation](https://letsencrypt.org/docs/)
- [Ubuntu Server Guide](https://ubuntu.com/server/docs)

---

**Удачного деплоя! 🚀**

_Последнее обновление: 25 декабря 2025_
